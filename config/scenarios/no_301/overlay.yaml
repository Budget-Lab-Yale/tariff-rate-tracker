# Counterfactual: remove Section 301 inputs before the revision is calculated.
# rate_301_cs is all-zero at baseline; add section_301_content_split here if
# that separate flavor is ever activated and should also be removed.
disabled_authorities: [section_301]
